---
description: Independently verify a completed feature using codex or gemini CLI as an out-of-model evaluator
---

Use the `harness-engineer:harness-verify` skill to verify feature $ARGUMENTS. The verifier must be a different coding CLI (codex or gemini) — NOT the current Claude session or a Claude subagent — so the verdict is free of self-validation bias.
