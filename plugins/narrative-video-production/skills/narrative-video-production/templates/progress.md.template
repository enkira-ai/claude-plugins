# 南大物理 02级 · 毕业20周年聚会视频 — 项目进度

> **目标**：以 30 秒 draft（`2026nju-min.mp4` / `2026nju-final.mp4`）为模板，扩展为包含全班同学的完整版聚会视频。
> **核心叙事**：量子纠缠 — 当年大家共处一室（强纠缠态）；毕业 20 年各自演化出不同轨迹（退相干）；2026 聚会再次相聚形成新节点（再纠缠）；未来未知，但守恒量是「我爱物理」。视觉母题：`S = k · log W`。

---

## 0. 工作目录与素材清单

工作目录：`/Users/bwen/Downloads/nju/`

| 类别 | 路径 | 数量 | 备注 |
|------|------|------|------|
| 在校时照片 | `在校时/` | ~166 张（含子目录：`名山/` 23, `本科照片/` 13, `2002-2006/` 87, `2005-07-17~20海门实习/` 15） | 数量会继续增加 |
| 毕业后照片 | `毕业后/` | ~27 张（含子目录 `物理02级毕业20周年_郭晓宇5班_邢星2班近照/`、`2008/`） | 数量会继续增加 |
| 同学视频 | `视频/` | 7 个 | 见 §3 |
| Draft 工程 | `project/` | Remotion/JSX 工程 | `scenes/scenes.jsx`、`animations.jsx`、`primitives.jsx` |
| Draft 成片 | `2026nju-min.mp4` / `2026nju-final.mp4` | 30s | 模板 |
| 背景音乐 | `2026nju-bmg1.mp3`、`2026nju-bmg2.mp3` | 2 条 | 备选 |

**工具盘点**（本机 macOS / Apple M4）：
- ✅ `ffmpeg` 已安装（`/opt/homebrew/bin/ffmpeg`）
- ✅ **STT：Deepgram**（key 在 `/Users/bwen/ubuntu-share/Panbot/.env` 的 `DEEPGRAM_API_KEY`）。`nova-3` 多语言模型，云端，速度比本地 whisper 快得多。
- ✅ **打标：Haiku 子 agent vision**。通过 `Agent(subagent_type=general-purpose, model=haiku)` 派批，子 agent 用 `Read` 工具直接看缩略图输出结构化 JSON。**无需额外 API key**，走 Claude Code 自身额度。

---

## 1. 任务总览（Phase Map）

```
Phase A · 素材清点          [in_progress]  ← 当前
Phase B · 照片归类与筛选     [pending]
Phase C · 视频归一与转写     [pending]
Phase D · 故事板与节奏设计   [pending]
Phase E · Remotion 扩展实现  [pending]
Phase F · 成片渲染与微调     [pending]
```

---

## 2. Phase B · 照片归类 / 筛选 — 详细方案

> 目标：把 ~190+ 张照片归到清晰的类别，每类挑出精品（或汇总进子文件夹给你最终挑），形成可被 Remotion 使用的资产清单。

### B.1 缩略图 + 视觉打标（节省 token）

1. 用 `sips` 把所有照片批量缩到长边 ≤ 512px 存到 `_thumbs/`（保持原目录结构，仅供 LLM 看，不动原图）。
2. 把 thumbs 列表分批（~20 张/批），每批派一个 `Agent(subagent_type=general-purpose, model=haiku)` 子任务：让子 agent 用 `Read` 读图，逐张输出结构化 JSON 行：
   - `filename` (相对路径), `era` (在校/毕业后), `scene` (实验室/教室/食堂/山岳/校园/合影/婚礼/亲子/工作/旅行/书法/其它), `people_count`, `mood` (合影正式/抓拍/风景/纪念物), `quality` (1-5，构图+清晰度), `caption_zh` (一句中文), `dedup_key` (粗略相似度标签，供 §B.3 使用)。
3. 主会话把所有子 agent 返回的 JSON 行拼成 `photos_index.csv`，供后续筛选/排版查询。

> 我**不能可靠地认人**（多数同学我没有 reference，且像素受限），所以 `people_count` 给出，但具体姓名仍需你来确认。

### B.2 类别建议（先以现有素材推断，后续可调）

- 入学/初识：古林公园野炊联谊、教室、寝室
- 课堂/实验：experiment.JPG、实验室、`本科照片/` 部分
- **「不言自合」群山行**（你提到的精彩主题）：`名山/`，按地点（黄山/泰山/…）再细分，每地点 1-2 张精选
- 校园生活：江心洲、浦口雪、滁州行、栖霞、中山陵
- 班级合影：1班/2班/5班 等
- 学生工作 / 文体：学生会、足球队、实习
- 海门实习：`2005-07-17~20海门实习/`
- 毕业典礼：毕业.JPG、grad_caps、grad_party 等
- **20 年演化轨迹（evolution）**：`evo_2006…evo_2026.jpg` — 这是 draft 里已有的"年度切片"，留作主线
- 现况近照：郭晓宇/邢星/严迪新/王海静/包文中 等
- 书法/纪念物：包主任墨宝、道法书法 — 作为转场静物

### B.3 同类去重

对每个类别，按 `dedup_key` + `quality` 分组：
- 类内 ≤ 6 张：直接保留全部进精选池。
- 类内 > 6 张：把相似的（同场景/同活动）放到 `_review/<类别>/<组号>/`，由你最终挑 1-2 张；其余仍保留在原位不删。

---

## 3. Phase C · 视频归一 + 音轨 + STT

### C.1 视频清单

| 文件 | 大小 | 用途（建议） |
|------|------|--------------|
| `2026nju-bo-wen-1.mp4` | 776 KB | 已 H.264 压缩，参考码率/分辨率基准 |
| `陈公羽二班021122005.MP4` | 9.4 MB | **开篇候选**：找回当年 laptop + 试密码解锁 |
| `1班-黄张成.mp4` | 5.2 MB | **结尾候选**：拿出当年 laptop + 魔兽 outro |
| `于婉卿.mp4` | 37.7 MB | 中段穿插 |
| `李成栋.mp4` | 3.4 MB | 中段穿插 |
| `张智建5班+汪晨璐2班.mp4` | 18.5 MB | 中段穿插（双人，可单独段落） |
| `郭晓宇邢星_毕业二十年祝福_260501.mov` | 33.2 MB | 中段穿插（双人祝福） |

### C.2 流程

1. `ffprobe` 每个文件 → 记录 `duration / codec / fps / bitrate / size` 进 `videos_index.csv`。
2. 统一封装：1080p / H.264 / AAC 48 kHz / CRF 20 / yuv420p；扫描方向归一；输出到 `视频/_normalized/`。以 `2026nju-bo-wen-1.mp4` 的参数为基准。
3. 提取音轨：`ffmpeg -i <in> -vn -acodec libmp3lame -q:a 2 视频/_audio/<name>.mp3`。
4. STT：**Deepgram** `nova-3` 多语言（key 见 §0），输出 JSON + SRT 到 `视频/_stt/`。带关键词提示「南京大学 物理 02级 魔兽世界 量子 实验」减少专有名词错字。
5. 把每段 STT 摘成 3-5 个 bullet 写入 `videos_index.csv` 的 `summary` 列，用于 §D 编排。

---

## 4. Phase D · 故事板 / 节奏 (v1，~6:06，已与用户初步对齐)

策略：视频全长保留（仅于婉卿+严迪新双人段剪到 27.6s，文件 `视频/_normalized/于婉卿_final.mp4`）；视频做为照片块的隔断节点；尽量让视频内容与前后照片块有语义链接；「我爱物理」作为反复出现的守恒量 motif。

| 时段 | 内容 | 时长 | 链接 |
|------|------|------|------|
| 0:00 | 片头 SceneTitle | 5s | — |
| 0:05 | `bo-wen-1` 引子 | 10s | 立纲领 |
| 0:15 | 黑板 `S=k·logW` + 「解锁记忆」card | 5s | 转入主题 |
| 0:20 | ★ OPEN：`陈公羽` 找回笔记本 | 72s | — |
| 1:32 | 📷 Block 1 — 解锁记忆（18 张，节奏曲线见下） | 14s | 笔记本→老电脑 |
| 1:44 | `李成栋` 诗意「我爱无比」 | 15s | — |
| 1:59 | 📷 Block 2 — 课堂实验+食堂+学生活动（5 张） | 15s | 共同的记忆 |
| 2:14 | `于婉卿_final` 西雅图（双人含严迪新） | 28s | — |
| 2:42 | 📷 Block 3 ★ 不言自合·群山行（10 张） | 30s | 各奔远方 |
| 3:12 | `张智建+汪晨璐` 波特兰「我爱物理」 | 39s | — |
| 3:51 | 📷 Block 4 — 班级毕业合影+海门实习+书法（5 张） | 15s | 物理之根 |
| 4:06 | `郭晓宇+邢星` 旧金山 | 48s | — |
| 4:54 | 📷 Block 5 — `evo_2006…evo_2026` 年表（draft 已有动效） | 28s | 毕业后轨迹 |
| 5:22 | 📷 Block 6 — 重访校园+现况近照（4 张） | 10s | 再纠缠节点 |
| 5:32 | ★ CLOSE：`黄张成` 笔记本+魔兽+「玩物理永远年轻」 | 29s | — |
| 6:01 | 片尾 entropy outro + S=k·logW | 5s | — |
| | **总计** | **~6:06** | |

「我爱物理」motif 出现：4:06 张智建 → 5:32 黄张成 → 收尾 `S=k·logW` 守恒量画面。

### Block 5 路径积分 design (LOCKED)

**核心比喻**：费曼路径积分。`evo_2006…2026` 是 user（文博，常驻 NYC）一个人走的轨迹，但 frame 成「Σ 路径中的一条」。「不言自合」（block3）已经埋了同样的 idea —— 同一 \|Ψ⟩ 支配下的不同采样路径。

**黑板公式**：写实版 `⟨f|i⟩ = ∫ 𝒟x · e^(iS/ℏ)`，置顶或底部小字常驻。

**28s 画面分段**：
- 0-3s：公式浮现 + 字幕「我走的这一条 —— 是其中一条。」
- 3-22s：`evo_2006…2026` 推进（draft 已有动效）。**画面边缘半透明叠加"幽灵路径"层**：抽象曲线 + 同学去向城市名 (Seattle / SF / Portland / 已确认；其余待用户补充)
- 22-26s：所有 ghost paths 向 2026 节点收束 = 重纠缠
- 26-28s：字幕「未观测，不等于不存在」→「同一个 Ψ，不同的采样」→「守恒量：我爱物理」→ 进 block6

**Ghost path 城市清单**（用户提供 2026-05-02）：
- 美国西海岸：Seattle (于婉卿+严迪新) / San Francisco (郭晓宇+邢星) / Portland (张智建+汪晨璐)
- 美国中南：Houston / Phoenix
- 美国东海岸：**NYC (用户文博)** / Boston / DC
- 海外：Tokyo (Japan) / Paris
- 国内：北京 / 上海 / 广州 / 深圳 / 南京
- 其它（未知，留白）：用 `…` 或半透明问号占位，暗示未观测到的本征态

**Ghost path 视觉规则**：
- NYC 是用户路径终点，应该和 evo_2006-2026 主路径**实线相连**
- 其它 13 个城市用半透明虚线（alpha 0.12-0.25）从 2006 节点散射出去
- 22-26s 收束动画时全部向 2026 节点汇集

**待办**：
- [ ] 在 Remotion 中实现 ghost path layer（贝塞尔曲线 + 城市文字 + 透明度噪声）
- [ ] block5 与 draft `project/photos/evo_*.jpg` 的具体动效衔接
- [ ] 公式 `⟨f|i⟩ = ∫ 𝒟x · e^(iS/ℏ)` 用 KaTeX 预渲染或 SVG 字体

### Block 3 子文件夹更新（用户反馈后）

- 黄山子文件夹：`黄山.jpg` (到此一游石碑) 和 `不言自合_露营_06.jpg`（同一群人在山上躺着搞怪）经用户确认确为黄山
- 火车上：经用户确认是火车（Haiku 把火车看成船舱）
- `各种搞怪/` 整组挪到 block2
- 根目录 3 张挪入 `各处游玩/`，不标具体地点
- 当前 block3 = 31 张：黄山(4) / 泰山(3) / 栖霞·明孝陵(4) / 徽杭古道(1) / 火车上(2) / 各处游玩(12) / 骑行(5)

### Slide 标题策略 LOCKED (2026-05-02)

| Block | 中文主 | 中文副 | 英文小标 |
|-------|--------|--------|----------|
| 1 | 解锁记忆 | 找回 20 年前的密码 | `FIND THE PASSWORD` |
| 2 | 共同的回忆 | 能文能武 · 吃喝玩乐 | `LIBERAL · SCHOLARLY · EVERYDAY` |
| 3 | 三川五岳 · 游学四方 | 同一个 Ψ，不同的采样 | `PATHS · INDEPENDENTLY · CHOSEN` |
| 4 | 物理之根 | 我爱物理 · 不变量 | `PHYSICS · THE INVARIANT` |
| 5 | `⟨f\|i⟩ = ∫ 𝒟x · e^(iS/ℏ)` | 每个粒子都有了自己的不同轨迹 ／ 殊途同归 | `FEYNMAN · PATH INTEGRAL` |
| 6 | 量子纠缠 · 2026 | 穿越时空的再相聚 | `RE-ENTANGLED · 2026` |

**Block 3 角标地名**：仅在 黄山(2 张明确) / 泰山(2 张) / 栖霞山(1) / 明孝陵(2) / 徽杭古道(1) 上短暂显示 0.5-1s。其它（火车上/各处游玩/骑行）不打地点，受主标覆盖。

### 全片物理公式 / 概念清单 (LOCKED)

| # | 时段 | 公式 | 物理概念 | 中文 / 成语 |
|---|------|------|---------|------------|
| 1 | 片头 Scene 1 | `S = k · log W` | Boltzmann 熵 | 有序态 (motif) |
| 2 | 持续 (左上) | `S = k · log W` | (常驻 motif) | — |
| 3 | Block 3 转场 | `δS = 0` | Hamilton 最小作用量 | 不约而同 / 经典极限 |
| 4 | Block 5 转场 | `Σ_paths · e^(iS/ℏ)` | sum-over-paths 引子 | 殊途同归 / 量子展开 |
| 5 | Block 5 主 | `⟨f\|i⟩ = ∫𝒟x · e^(iS/ℏ)` | Feynman 路径积分 | 每个粒子都有了自己的不同轨迹 |
| 6 | Block 5 后转场 (3s, NEW) | morph: `∫𝒟x e^(iS/ℏ)` → `\|ψ⟩=(\|南京⟩+\|美国⟩)/√2` | path integral 散掉 → Bell state 量子纠缠 | **同归** / 重纠缠 |
| 7 | 黄张成 CLOSE | — | "玩物理永远年轻" | 我爱物理 (motif) |
| 8 | 最后一帧 | (motif 持续) + 「我爱物理」 | 守恒量 invariant | 我爱物理 |

**重大重构 (2026-05-02 晚)**：
- 所有转场卡 1.5s → **3s**（让观众看清公式含义）
- Block 5 前转场：`殊途同归` → **`殊途`**（仅"殊途"）
- Block 5 主块字幕：「每个粒子都有了自己的不同轨迹」**早出现**（0.6s 起 reveal），删除「殊途同归」尾
- **Block 6 整段并入 Block 5 ghost layer** —— 原 17 张校友近照变成"其他粒子轨迹"的浮动闪现（与 14 个城市同层）
- **Block 5 后新增「同归」转场 (3s)**：路径积分公式散掉 → Bell state 浮现，含 "RE-ENTANGLED · 2026"
- Outro 缩短到 4s（Bell state 已在「同归」展示，outro 只保留 文博 NYC signoff + 「我爱物理」大字）
- 用户已删除原 block6/家庭近照/重访母校_02.jpg（与 evo timeline 重复）

**叙事弧**（成语对偶 + 公式演化）：
```
殊途 (Σ_paths · e^iS/ℏ) ────── 同归 (|ψ⟩ Bell state)
    每个粒子的轨迹                    重纠缠为一体
   (block 5 主块内)                  (block 5 后转场)
```

物理叙事弧：熵 → Hamilton (经典) → 路径积分 (量子) → 量子纠缠 → 守恒量。

### 转场卡 design (locked)

**Block 3 转场卡 (1.5s)**：
```
          不约而同              ← 大字 chalk serifCn
        ─────────────
          δS = 0                ← 中字 chalk serifEn italic
   Hamilton's Principle         ← 小字 mono, alpha 0.6
```

**Block 5 转场卡 (1.5s)**：
```
          殊途同归              ← 大字 chalk serifCn
        ─────────────
       Σ_paths · e^(iS/ℏ)       ← 中字 chalk serifEn italic
    Sum Over Histories          ← 小字 mono, alpha 0.6
```

设计意图：两张转场卡形成对仗 + 公式从离散 Σ 变成 block5 主公式的连续 ∫𝒟x。

### Block 1 节奏曲线 (locked)

照片节奏隐喻"密码解锁后记忆从混沌到稳定"的物理过程：

```
       笔记本回响_01            ←  长 hold (2.5s) "trigger"
            ↓
       笔记本回响_02-04         ←  快闪 (0.15s 每张, 共 0.45s)
            ↓
       寝室回响 + 少数人合影     ←  快→慢，碎片化 (decelerating, 13 张, 共 ~9s)
                                    指数式减速：0.3s → 1.0s
            ↓
       古林联谊_01              ←  长 hold (2s) "stable" 入学初心
                                    → 进入 block2

总时长 ≈ 14s
```

数学：dwell_time(i) = t_min + (t_max - t_min) · (i/N)^1.5，其中 i 在快闪段从 1 到 13，t_min=0.3s, t_max=1.0s

视觉效果：从混沌闪烁逐渐收敛 → 在古林联谊（大学第一帧记忆）处稳定 → 表征"记忆锁相完成"。完美呼应陈公羽的"找回密码"叙事。

### 公式渲染机制（确认）

draft 用 `primitives.jsx:177` 的 `ChalkFormula`（chalk italic + clip-path 反向揭示），不依赖 KaTeX。
Block 5 路径积分实现：扩展 `ChalkFormula` 接受 JSX children → 内含 `<sup>iS/ℏ</sup>`。Unicode `⟨ ⟩ ∫ 𝒟 ℏ ·` 直接显示。

(原 v0 storyboard 已废弃，下面是早期的 fallback 草稿)

```
① OPEN  ── 陈公羽：找回 laptop / 试密码（0 ~ 25s）
            ↳ 视觉钩子："解锁当年" → 进入回忆
② BODY  ── 在校段（强纠缠态 |Ψ⟩）
            • 入学 / 古林联谊
            • 课堂 + 实验 (S = k log W 写在黑板)
            • 班级合影 (1/2/3/4/5 班蒙太奇)
            • 「不言自合」群山行 ★ 重点小节，多页快切
            • 校园生活 / 海门实习 / 毕业典礼
            ↳ 中间穿插 1-2 段同学视频（3-8s 截选）
③ DRIFT ── 演化年表 evo_2006 … evo_2026（draft 已有动效）
            • 退相干：每人独自轨迹
            • 穿插于婉卿 / 李成栋 / 张智建+汪晨璐 / 郭晓宇+邢星 视频中长度合适的片段
④ RE-ENT ── 2026 聚会照片 + 现况近照（再纠缠节点）
⑤ CLOSE ── 黄张成：laptop + 魔兽 outro
            ↳ 守恒量：「我爱物理」 + S = k log W 持续燃烧
```

**总时长目标**：素材汇总后再定。粗估
- 视频片段总长 ≈ 各原视频精选 5-10s × 7 ≈ 50-70s
- 照片节奏 0.6-1.2s/张，精选若 60-80 张 ≈ 60-90s
- 加片头/转场/年表 ≈ 30s
- → **建议成片 3-4 分钟**（150-240s）。draft 30s 节奏会在扩展版被拉宽，避免视觉过载。

---

## 5. Phase E · Remotion 扩展

- 复用 `project/scenes/primitives.jsx` 与 `animations.jsx` 的 chalk / 公式 / easing。
- 新增 scenes：`SceneOpenLaptop`, `SceneClassMontage`, `SceneMountains`, `SceneEvolution`(已存在), `SceneReunion`, `SceneClose`.
- 新增 `<VideoClip src start end fadeIn fadeOut/>` primitive 用于嵌入同学视频。
- 资产管线：`project/photos/` 软链或拷贝精选过来，命名规范 `<类别>_<序号>_<人物或地点>.jpg`。

---

## 6. 待办（Todo） / 当前 Blockers

- [x] 打标方案：Haiku 子 agent vision（无需额外 key）。
- [x] STT 方案：Deepgram `nova-3`，key 已就位。
- [ ] 用户最终对类别命名 / 取舍的反馈。
- [ ] 收集是否还有更多视频要加入。
- [ ] **下一步**：进入 Phase B —— 生成 `_thumbs/`，派 Haiku 子 agent 打标，产出 `photos_index.csv`。

---

## 7. 错误 / 异常日志

| 时间 | 现象 | 排查 / 处理 |
|------|------|-------------|
| 2026-05-02 | `ollama` 未安装 | 用户改指 Deepgram；whisper 方案废弃 |

---

## 8. Session Log

- **2026-05-02 早**：初次清点素材；建立本 `progress.md`；与用户对齐"量子纠缠"叙事框架与 Phase A-F 路线图。素材数量在变化中（在校 166 / 毕业后 27 / 视频 7），下一步等待用户确认是否进入 Phase B 自动化打标。
- **2026-05-02 中**：用户决策：STT → Deepgram；打标 → Haiku 子 agent；绕开 Anthropic API key 与 ollama 安装。
- **2026-05-02 下**：Phase B v1 完成 ——
  - 修正：Downloads 下有两个 `nju` 文件夹（一个名字尾部带空格），真目录是 `/Users/bwen/Downloads/nju /`（含尾空格）。空的同名错目录已清。
  - `_thumbs/` 共 193 张缩略图（11 MB，长边 ≤512px）。
  - 10 个 `Agent(model=haiku)` 并发打标，193 张零失败。
  - `_tags/batch_*.jsonl` → `photos_index.csv`（字段：`category, era, scene, people_count, mood, quality, caption_zh, dedup_key, notes`）。
  - 共 13 个 macro category（详见 §9）。

## 9. Phase B 结果 · 照片类别分布（193 张）

| # | 类别 | 数量 | 备注 |
|---|------|------|------|
| 02 | 宿舍生活 | 28 | **多 q2 低质**，可大幅删（13×q2，13×q3，2×q4） |
| 03 | 课堂实验 | 6 | 全保留 |
| 04 | 食堂聚餐 | 10 | 多高质，去重后留 4-5 |
| 05 | 校园生活 | 46 | **最大类**，需细分（古迹/牌坊/书摊/塔楼/草坪/校庆/学生活动…） |
| 06 | 校园外出春游 | 4 | 全保留 |
| 10 | 班级毕业合影 | 13 | 各班合影齐 + `物理系毕业合影.jpg` 大全家福 |
| 11 | 海门实习 | 15 | 工厂参观 + 街头志愿活动两条线 |
| 12 | **不言自合·群山行** ★ | 40 | **重点小节**。子题：黄山 / 泰山 / 栖霞山 / 明孝陵 / 徽杭古道 / 露营 / 骑车 |
| 13 | 书法纪念物 | 1 | 包主任墨宝 |
| 20 | 毕业后-重访亲子 | 6 | 浦口校门 / 北大楼 / 物理楼 / 13栋宿舍 |
| 21 | 毕业后-职场 | 1 | 校友足球队 |
| 22 | 毕业后-聚餐近照 | 17 | 含 `evo_2006-2026` 年表素材 |
| 99 | 杂项 | 6 | 待分流 |
|   | **合计** | **193** | |

### 质量分布

q5: 3 ｜ q4: 121（主力池）｜ q3: 56 ｜ q2: 13（集中在宿舍）

### Phase B 已完成 → 进入 Phase B' 选片

- 用户选择放弃 contact sheet 路径，改为按 storyboard block 整理到 folder structure，让用户在 Finder 里直接删除不要的。
- `_curated/` 已生成（172 张）：
  - `block1_解锁记忆/` (18) — 笔记本回响(1) / 古林联谊(2) / 寝室回响(15)
  - `block2_共同的记忆/` (54) — 课堂实验(6) / 食堂聚餐(10) / 学生工作(4) / 校园生活(34)
  - `block3_不言自合·群山行/` (44) — 黄山(2) / 泰山(4) / 栖霞·明孝陵(4) / 徽杭古道(1) / 露营骑行(7) / 其它山岳(26)
  - `block4_物理之根/` (34) — 班级合影(4) / 毕业典礼(14) / 海门实习(15) / 书法墨宝(1)
  - `block5_演化年表/` — 仅 README，使用 draft `project/photos/evo_*.jpg`
  - `block6_再纠缠/` (19) — 重访母校(6) / 校友近照(6) / 家庭近照(7)
  - `99_杂项/` (3) — 分类不清，待用户决定
- 文件名 = `<slide_title>_NN.<ext>`；完整 manifest 在 `_curated/manifest.csv`。
- Block 5 的 9 张 `毕业后/2006-2026.jpg` 已自动跳过避免与 draft `evo_*.jpg` 重复。
- 已知偏差：
  - `黄山` 只 2 张：Haiku 多数 `名山/` 照片用了泛化 dedup_key "山岳合影..."，没识别具体山名；用户可在 Finder 里手动拖动重分。
  - `笔记本回响` 只 1 张：宿舍电脑类照片大多 q2 被剔除。如果需要可放宽 quality 门槛重跑。
